{
  "name": "clean-node-api",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:heldersonvieira/clean-node-api.git",
  "author": "Helderson Vieira <vieirahelder.fort@gmail.com>",
  "license": "MIT",
  "scripts": {
    "test": "jest --colors --silent",
    "test:unit": "yarn test -- --watch -c jest-unit-config.js",
    "test:int": "yarn test -- --watch -c jest-int-config.js",
    "test:staged": "yarn test -- --passWithNoTests --findRelatedTests",
    "test:ci": "yarn test -- --coverage"
  },
  "devDependencies": {
    "@shelf/jest-mongodb": "^3.0.2",
    "husky": "4.0.0-beta.5",
    "jest": "^28.1.0",
    "lint-staged": "^12.5.0"
  },
  "dependencies": {
    "dotenv": "^16.0.1",
    "jsonwebtoken": "^8.5.1",
    "mongodb": "^4.7.0"
  }
}
