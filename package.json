{
  "name": "clean-node-api",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:heldersonvieira/clean-node-api.git",
  "author": "Helderson Vieira <vieirahelder.fort@gmail.com>",
  "license": "MIT",
  "scripts": {
    "test": "jest --colors",
    "test:unit": "yarn test -- --watch --silent -c jest-unit-config.js",
    "test:int": "yarn test -- --watch --silent -c jest-int-config.js",
    "test:staged": "yarn test -- --passWithNoTests --findRelatedTests",
    "test:ci": "yarn test -- --coverage"
  },
  "devDependencies": {
    "husky": "4.0.0-beta.5",
    "jest": "^28.1.0",
    "lint-staged": "^12.5.0"
  }
}
